{
  "format": ".env",
  "file": ".env",
  "config": {
    "PORT": {
      "description": "Which port should the server run on?",
      "default": "3001",
      "required": true
    },
    "ENVIRON": {
      "description": "Environment (DEV or PROD)",
      "default": "DEV",
      "required": true
    }
  }
}
